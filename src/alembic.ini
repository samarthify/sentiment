# ... other alembic config ...

# Database connection URL. Use ${VAR_NAME} to read from environment variables.
# Example: sqlalchemy.url = postgresql://user:pass@localhost/dbname
#          sqlalchemy.url = driver://user:pass@host/database

sqlalchemy.url = ${DATABASE_URL}

# ... rest of the file ... 